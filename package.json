{
  "name": "ai-agent-governance",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend run start",
    "test": "npm --prefix backend test",
    "test:contracts": "npm --prefix backend run test:contracts"
  },
  "dependencies": {
    "openai": "^6.34.0"
  }
}
